// src/index.js

import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
  return (
    <span>
      <h1>Hello, React!</h1>
    </span>
  );
};

ReactDOM.render(<App />, document.body);
